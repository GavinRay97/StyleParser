#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sast/bin/sast-parse" "$@"
  ret=$?
else 
  node  "$basedir/../sast/bin/sast-parse" "$@"
  ret=$?
fi
exit $ret
